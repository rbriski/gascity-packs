# Deprecated full gstack graph

These agents, workflow assets, and formulas are retained only as historical
implementation evidence. They are outside the Pack V2 discovery directories
and must not be imported, launched, or used as the ordinary delivery path.

Use `../skills/gstack-lite` plus selective risk skills instead.
