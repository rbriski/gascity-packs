Finalize the gstack QA review.

Verify the latest QA loop approved the product behavior and record the
approved QA summary on the workflow root at `gc.build.qa_summary_path` for
release readiness and the final sprint report.

Close with `gc.outcome=pass`.

Do not invoke provider-native subagents.
