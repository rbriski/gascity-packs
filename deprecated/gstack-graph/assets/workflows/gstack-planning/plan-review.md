Run the gstack plan review lanes and update `{{plan_path}}` with approved
findings.

Current interaction_mode is {{interaction_mode}}. Use reportable assumptions in
autonomous mode and explicit human gates in interactive mode.

Do not invoke provider-native subagents.
