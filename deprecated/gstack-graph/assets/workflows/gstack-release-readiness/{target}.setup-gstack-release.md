Prepare the gstack release readiness context.

Collect the sprint report inputs, documentation inventory, test evidence,
review approval, QA approval, publish flags, and deployment constraints.

Current review_mode is {{review_mode}}. In report mode, do not push, open PRs,
merge, or deploy; write readiness findings only.

Close with `gc.outcome=pass`.

Do not invoke provider-native subagents. Gas City graph lanes are the
delegation mechanism.
