Drain the gstack implementation convoy.

assigned implementation work should route to {{implementation_target}}. The
convoy contains the implementation beads created from the approved plan; each
worker should use the gstack work discipline and leave proof in the requested
summary artifact.

Confirm the drain is using separate-session context and exclusive member
access. Do not edit source files from this control bead.

Close with `gc.outcome=pass` once the drain control work is complete.

Do not invoke provider-native subagents. The Gas City drain is the delegation
mechanism.
