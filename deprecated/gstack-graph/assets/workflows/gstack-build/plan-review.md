Prepare for the gstack plan-review fanout.

The next graph work runs CEO/founder, design, engineering, and developer
experience lanes. Ensure the plan path is recorded on the workflow root and
that the review context includes requirements, constraints, acceptance
criteria, test commands, and open decisions.

Current interaction_mode is {{interaction_mode}}. Interactive gates should be
handled by the child lanes through mail or passive wait metadata; autonomous
runs should record assumptions instead of blocking on missing preferences.

Close this stage with `gc.outcome=pass` after the plan-review context is ready.

Do not invoke provider-native subagents. The Gas City fanout is the delegation
mechanism.
